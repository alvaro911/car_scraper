var React = require('react')

class Header extends React.Component{
  render(){
    return (
    <div>
      <header>
        <h2>CarStock</h2>
      </header>
    </div>
  )}
}

module.exports = Header
